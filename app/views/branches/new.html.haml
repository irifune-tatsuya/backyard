.branch__body
  新規支社登録
  .branch__body__form
    = form_for @branch do |f|
      .field
        .field-label
          = f.label "支社名"
        .field-input
          = f.text_field :name, autofocus: true
      .actions
        = f.submit "登録", class: 'btn'