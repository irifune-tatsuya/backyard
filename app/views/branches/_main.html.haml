.main
  =render 'branches/mainheader'
  .main__body
    %table 
      %tr
        %th 氏名
        %th 入社日
        %th{id: "short"}
          付与日
        %th{id: "short"}
          勤続年数
        %th{id: "short"}
          繰越日数
        %th{id: "short"}
          消化日数
        %th{id: "short"}
          残日数
        - @employees.each do |employee|
          %tr
            %th
              = employee.name
            %th
              =employee.hire_date
            %th{id: "short"}
              10/01
            %th{id: "short"}
              0.5年
            %th{id: "short"}
              10
            %th{id: "short"}
              2
            %th{id: "short"}
              8