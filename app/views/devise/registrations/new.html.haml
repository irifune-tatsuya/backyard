#account-page.account-page
  .account-page__inner.clearfix
    .account-page__inner--left.account-page__header
      %h3 有給休暇管理ツール「Backyard」
      %h5 「Backyard」へようこそ。こちらは、プログラミングスクール「TECHCAMP」の75期生のいりふねが、カリキュラムの一環で開発したアプリケーションです。どなたでも自由にご利用していただけますが、くれぐれも個人情報を登録しないようにお願い申し上げます。Emailに関しては「aaa@aaa」など文字列の間に「＠」が使用されていれば新規登録を行うことができます。ただし、Emailは他のユーザーと重複したものは使用できません。以上の点についてご確認いただいた方は、中を覗いてみて下さい。
      = render "devise/shared/links"
    .account-page__inner--right.user-form
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        = devise_error_messages!
        .field
          .field-label
            = f.label "名前"
          .field-input
            = f.text_field :name, autofocus: true
        .field
          .field-label
            = f.label :email
          .field-input
            = f.email_field :email
        .field
          .field-label
            = f.label "パスワード"
            %i (英数字8文字以上)
          .field-input
            = f.password_field :password, autocomplete: "off"
        .field
          .field-label
            = f.label "パスワードを再入力"
          .field-input
            = f.password_field :password_confirmation, autocomplete: "off"
        .actions
          = f.submit "アカウント作成", class: 'btn'
